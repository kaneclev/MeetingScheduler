public class Main {
    /*
    todo:
        creating a program that takes in a file of times and then returns free times within the schedule
     */
    public static void main(String[] args) {
      if(args.length != 0) {
          fileReader fr = new  fileReader(args[0]);
      }
    }
}